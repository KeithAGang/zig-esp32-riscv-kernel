// src/chip/p4/layout.zig — memory map constants for p4.
//
// RAM/flash base + length, MMIO bases, stack top, timer frequency.
// Numbers only, no logic — the linker script and mod.zig both read from here.
//
// EMPTY STUB — not yet written.
